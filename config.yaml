# Splunk Connect for SNMP poller
# Sample Configuration file
ipv4: True
ipv6: False
communities:
  v1:
    - public
    - "my-area"

profiles:
  router:
    varBinds: ['sysDescr', 'uptime', 'name', 'routingTable']
  atm:
    varBinds: ['sysDescr', 'cashLeft', 'transactionCount']